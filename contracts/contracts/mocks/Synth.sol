pragma solidity ^0.4.25;

contract Synth {

    function transferAndSettle(address, uint) public returns (bool) {
        return true;
    }

    function transferFrom(address, address, uint) public returns (bool) {
        return true;
    }

}